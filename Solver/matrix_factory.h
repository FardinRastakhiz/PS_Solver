#pragma once

#include "SharedTypes.h"

namespace ses {
	void create_matrix(int num_rows, int num_cols, int nnz, int* rows, int* cols, ScalarType* values, VI_SELL_MAT& matrix) {

	}

	void create_matrix(int num_rows, int num_cols, int nnz, int* rows, int* cols, ScalarType* values, VI_COMP_Mat& matrix) {

	}


	void create_matrix(int num_rows, int num_cols, int nnz, int* rows, int* cols, ScalarType* values, VI_COO_Mat& matrix) {

	}
}